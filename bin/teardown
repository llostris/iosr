#!/bin/bash -eu

source $OPENSHIFT_CARTRIDGE_SDK_BASH

client_message "Spark Cartridge: Teardown"
client_message "Stopping cluster, removing all cluster and job-related objects"

$OPENSHIFT_SPARK_DIR/bin/control stop

# rm -rf $OPENSHIFT_SPARK_DIR/*

exit 0